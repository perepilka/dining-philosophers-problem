# 🚀 Швидкий старт - Python

## 📦 Вимоги

- Python 3.6+
- Модулі: `threading`, `time`, `logging` (стандартна бібліотека)

**Не потребує компіляції та додаткових залежностей!**

---

## 🎯 Запуск

### Варіант 1: Пряма команда

```bash
# Deadlock (зависне!)
python3 dining_philosophers.py deadlock

# Hierarchy (працює)
python3 dining_philosophers.py hierarchy

# Asymmetric (працює)
python3 dining_philosophers.py asymmetric
```

### Варіант 2: Використання Makefile

```bash
# Переглянути всі команди
make help

# Запустити стратегії
make deadlock
make hierarchy
make asymmetric

# Тестування всіх працюючих стратегій
make test
```

---

## ⚙️ Конфігурація

Відкрий `dining_philosophers.py` та зміни константи на початку файлу:

```python
# ============================================================================
# CONFIGURATION
# ============================================================================
NUM_PHILOSOPHERS = 5        # Кількість філософів (змінюй це!)
MODE = "deadlock"           # Стратегія за замовчуванням
SIMULATION_TIME = 10        # Час симуляції в секундах
```

### Приклади налаштувань:

**Для демонстрації deadlock:**
```python
NUM_PHILOSOPHERS = 3
SIMULATION_TIME = 5
```

**Для довгих тестів:**
```python
NUM_PHILOSOPHERS = 10
SIMULATION_TIME = 30
```

---

## 📊 Очікувані результати

### ❌ Deadlock
```
❌ DEADLOCK DETECTED! Some philosophers are still waiting for forks.
Philosophers still blocked: 5
Total meals: 0
```

### ✅ Hierarchy / Asymmetric
```
✅ All philosophers finished successfully (No deadlock).
Philosophers still blocked: 0
Total meals: 50+ (залежить від часу симуляції)
```

---

## 💡 Корисні поради

1. **Для демонстрації deadlock** використовуй 3-5 філософів
2. **Для тестування працюючих рішень** можна збільшити до 10-100
3. **Для довшої симуляції** зміни `SIMULATION_TIME`
4. **Для звіту** запусти кожну стратегію і збережи логи

---

## 🎓 Для звіту/презентації

```bash
# 1. Покажи проблему
python3 dining_philosophers.py deadlock

# 2. Покажи рішення 1
python3 dining_philosophers.py hierarchy

# 3. Покажи рішення 2
python3 dining_philosophers.py asymmetric

# 4. Порівняй статистику
# Зберігай результати кожного запуску
```

---

**Успіхів! 🚀**
